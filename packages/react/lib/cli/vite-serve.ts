export async function viteServe(config?: Record<string, any>) {
  // TODO:
  // create and configure base server
  // create and configure vite server
  // configure hmr
  // configure and init watcher
  // manage requests
  // open browser with app running
}
